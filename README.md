# Flatten_Nested_JSON_Feed
Converting a JSON feed's nested format into a list of multiple elements where each element is a dictionary, i.e. a record. The metadata header and footnote is not included.
